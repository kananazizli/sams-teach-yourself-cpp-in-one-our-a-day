/* What is wrong in the following initialization?

auto age;

This code will fail the compilation stage because it does not contain an initialization.

The correct version would be: 

*/

auto age = 18;
